# Database (dev uses promptr/promptr, prod uses POSTGRES_PASSWORD secret)
DATABASE_URL=postgresql://promptr:promptr@localhost:5432/promptr
POSTGRES_PASSWORD=promptr

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT — generate a real key for production: openssl rand -hex 32
SECRET_KEY=change-me-in-production

# API Keys
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# App
DEBUG=true
CORS_ORIGINS=["http://localhost:3000"]

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
